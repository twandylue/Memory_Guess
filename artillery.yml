config:
  # target: "https://andyforfun.site"
  # target: "http://localhost:3000"
  target: "http://35.75.161.246"

  https:
    pool: 20
  phases:
    - duration: 20
      arrivalRate: 68
scenarios:
  - flow:
    - get:
        url: "/api/1.0/testLoad"
config:
  # target: "https://andyforfun.site"
  target: "http://localhost:3000"
  # target: "http://35.73.76.64"

  https:
    pool: 20
  phases:
    - duration: 20
      arrivalRate: 20
scenarios:
  - flow:
    - get:
        url: "/api/1.0/testLoad"